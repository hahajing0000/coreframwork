<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zy.frameworkapp">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".testview.EventActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testview.ImageLoaderActivity"></activity>
        <activity android:name=".testview.StorageActivity" />
        <activity android:name=".testview.MainActivity3" />
        <activity android:name=".testview.MainActivity2" />
        <activity android:name=".mvvm.view.UserCenterActivity" />
        <activity android:name=".MainActivity">

        </activity>

        <service
            android:name=".TestService"
            android:enabled="true" />
    </application>

</manifest>